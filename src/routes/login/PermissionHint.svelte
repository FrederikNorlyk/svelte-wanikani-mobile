<div class="permission-hint flex items-center rounded-3xl border-2 px-4 py-3">
	<p class="font-semibold">
		Make sure your token has the permission <code
			class="rounded-[0.7rem] px-2 py-[0.1rem] font-mono font-bold whitespace-nowrap"
			>reviews:create</code
		>
	</p>
</div>

<style>
	.permission-hint {
		border-color: light-dark(#f5c052, #c59a41);
		background: light-dark(#fff8dc, var(--background));
		box-shadow: inset 0 1px 0 light-dark(#fff, rgb(255 224 160 / 16%));
	}

	code {
		background: light-dark(#ffe5a5, #af842b);
	}
</style>
